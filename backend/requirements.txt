fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
psycopg2-binary==2.9.9
httpx>=0.27
itsdangerous>=2.1
# Bot-only deps (python-telegram-bot[ext], weasyprint, pydyf) removed when
# the telegram-bot service was disabled. To revive: restore them here and
# the telegram-bot block in docker-compose.yml.
